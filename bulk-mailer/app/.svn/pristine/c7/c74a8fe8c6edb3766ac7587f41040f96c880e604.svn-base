SELECT CONCAT(user_part, '@', domain_part) AS email
FROM activity, email
WHERE activity_type = 'unsubscribe'
AND activity.email_id = email.id;

/*
 grep -iv -f unsubscribed.csv all.csv > filtered.csv
*/